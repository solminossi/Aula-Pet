<%- include('partials/header') %>

<main>
  <section class="cadastro">
    <div class="container">
      <h1>Painel Administrativo</h1>

      <table>
        <caption>
          <div class="align-row">
            <h3>Produtos</h3>
            <a class="btn btn-success" href="/produtos/cadastrar">Adicionar</a>
          </div>
        </caption>

        <thead>
          <th>Produto</th>
          <th>Preço</th>
          <th>Descrição</th>
        </thead>

        <tbody>
          <% servicos.forEach(servico => { %>
          <tr>
            <td><%= produto.nome %> </td>
            <td><%= produto.valor %> </td>
            <td><%= produto.descricao %> </td>
            <td class="acoes">
              <form action="#" method="POST">
                <a href="/produtos/editar/<%= servico.id %>" class="btn">Editar</a>
                <button type="submit" class="btn btn-secondary">Deletar</button>
              </form>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </section>
</main>

<%- include('partials/footer') %>
